# Owner: Yu Fei - K3s and Monitoring
apiVersion: apps/v1
kind: Deployment
metadata:
  name: butler
  labels:
    app.kubernetes.io/name: butler
    app.kubernetes.io/component: web
spec:
  replicas: 2
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 300
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: butler
      app.kubernetes.io/component: web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: butler
        app.kubernetes.io/component: web
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: ghcr-pull
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: butler
          image: ghcr.io/frankrainrp/c270_fa_deployment:sha-REQUIRED
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: butler-config
            - secretRef:
                name: butler-runtime
          env:
            - name: TMPDIR
              value: /tmp
          startupProbe:
            httpGet:
              path: /api/live
              port: http
            periodSeconds: 2
            timeoutSeconds: 2
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/live
              port: http
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: temporary-files
              mountPath: /tmp
        - name: mongodb-exporter
          image: percona/mongodb_exporter:0.44.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: mongodb-metrics
              containerPort: 9216
              protocol: TCP
          env:
            - name: MONGODB_EXPORTER_URI
              valueFrom:
                secretKeyRef:
                  name: butler-mongodb-exporter
                  key: MONGODB_EXPORTER_URI
          args:
            - --mongodb.uri=$(MONGODB_EXPORTER_URI)
            - --web.listen-address=:9216
          readinessProbe:
            httpGet:
              path: /metrics
              port: mongodb-metrics
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /metrics
              port: mongodb-metrics
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: temporary-files
          emptyDir:
            sizeLimit: 128Mi
